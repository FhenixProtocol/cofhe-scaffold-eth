diff --git a/dist/web.cjs b/dist/web.cjs
index 92cb4dacf5158c3901fe4406d6e5048d06fd48fb..e31857e8fb94c0b2e6d198d29efba5fef69edb53 100644
--- a/dist/web.cjs
+++ b/dist/web.cjs
@@ -4055,6 +4055,20 @@ function createCofheClientBase(opts) {
   };
 }
 var createWebStorage = () => {
+  if (typeof document === "undefined") {
+    const memory = /* @__PURE__ */ new Map();
+    return {
+      getItem: async (name) => {
+        return memory.has(name) ? memory.get(name) : null;
+      },
+      setItem: async (name, value) => {
+        memory.set(name, value);
+      },
+      removeItem: async (name) => {
+        memory.delete(name);
+      }
+    };
+  }
   const client = iframeSharedStorage.constructClient({
     iframe: {
       src: "https://iframe-shared-storage.vercel.app/hub.html",
diff --git a/dist/web.js b/dist/web.js
index f0c54971e3dd4d10a890e7d832491bec652257c5..236cecc1ed8cb8ce3d682a6b530e908bf4dea7b3 100644
--- a/dist/web.js
+++ b/dist/web.js
@@ -5,6 +5,20 @@ import { constructClient } from 'iframe-shared-storage';
 import init, { init_panic_hook, TfheCompactPublicKey, CompactPkeCrs, ProvenCompactCiphertextList } from 'tfhe';
 
 var createWebStorage = () => {
+  if (typeof document === "undefined") {
+    const memory = new Map();
+    return {
+      getItem: async (name) => {
+        return memory.has(name) ? memory.get(name) : null;
+      },
+      setItem: async (name, value) => {
+        memory.set(name, value);
+      },
+      removeItem: async (name) => {
+        memory.delete(name);
+      }
+    };
+  }
   const client = constructClient({
     iframe: {
       src: "https://iframe-shared-storage.vercel.app/hub.html",
diff --git a/web/storage.ts b/web/storage.ts
index 8f3358a0cf095c7be436be658f86d6f1cf01b60d..3b5bfcc8489c7a21f39ba88b03e8bf0ecfc5aa41 100644
--- a/web/storage.ts
+++ b/web/storage.ts
@@ -5,6 +5,22 @@ import { constructClient } from 'iframe-shared-storage';
  * @returns IStorage implementation for browser environments
  */
 export const createWebStorage = (): IStorage => {
+  if (typeof document === 'undefined') {
+    const memory = new Map<string, any>();
+
+    return {
+      getItem: async (name: string) => {
+        return memory.has(name) ? memory.get(name) : null;
+      },
+      setItem: async (name: string, value: any) => {
+        memory.set(name, value);
+      },
+      removeItem: async (name: string) => {
+        memory.delete(name);
+      },
+    };
+  }
+
   const client = constructClient({
     iframe: {
       src: 'https://iframe-shared-storage.vercel.app/hub.html',
